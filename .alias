# -*- mode: sh -*-

# Note: $OPEN is defined in .bashrc

if test $(uname) = 'Linux'; then
    # if .alias is being used on Arch-Linux
    alias pacman='sudo pacman --color always'
    alias sp='sudo pacman -Syu;yaourt -Syu'
    alias mandb='sudo mandb'
    alias db='sudo updatedb'
    alias cl='sudo bleachbit'
    alias hex="od -t x1"
    alias gmail='$OPEN https://mail.google.com/mail/u/0/#inbox'
    alias use='~/.local/bin/use-5.0.0/bin/use'
    alias diff='diff -y --color=always'
else
    # .alias is being used on Darwin
    alias db='sudo /usr/libexec/locate.updatedb'
    alias m='$OPEN /Applications/Mail.app'
    function diff { colordiff -u "$@" | less -RF ;}
fi

# Aliaces that are Common to both

# these swap functions are used with aliaces
swap() { $1 $3; $2;}
swap_pipe() { $1 $3| $2;}
# function aliaces
cag() { cat "$1" | grep "$2"; } # cat and grep a keyword
hig() { history | grep $1; }
function ga() { git add $@ && git status -s;}
function gl() { git log --graph --pretty=format:'%Cred%h%Creset - %C(bold green)(%cr)%Creset%C(yellow) : %s %C(bold blue)%d%Creset' --abbrev-commit  $@ ;}
function github() {
    repository="$([ -d ".git" ] && echo $(basename $PWD) || echo "")"
    $OPEN "https://github.com/tati-z/"$repository
}
function grepp() { grep -rine $([ $# -eq 1 ] && echo  "$1 ." || echo $@);}
cd() { [ $# -eq 0 ] && builtin cd ~  ||  { builtin cd "$1" && ls -FG;} }

# custom aliaces
alias emacs='emacs-25.3'
alias python='python3.6'
alias ll='ls -alFgG'
alias la='ls -AF'
alias l='ls -CGF'
alias grep='grep --color=always'
alias gdb='gdb -q'
alias calc="sage"
alias bc='bc -q'
alias top='htop'
alias locate='swap_pipe "locate" "more" $1'
alias j='swap_pipe "joke.sh" "less" 8'
alias apropos='swap_pipe "apropos" "more" $1'
alias gh='git_do "github"'
alias gc='git_do "git commit -m"' #second argument will be added at the command line
alias gr='git rm -f'
alias gs='git status -s'
alias gu='git pull'
alias gp='git push'
alias gpo='git push origin master && gh'
alias gaa='git add --all && git status -s'
alias gd='git diff'
alias gss='git_dir "git status -s"' # gets the status of all remote repos from specified folder
alias gpp='git_dir "git pull"'   # pull all directories at once.
alias gll='git_dir "gl" "head -n 1"'   # pull all directories at once.
alias ..='cd ..'
alias ...='cd ../..'
alias .b='. ~/.bashrc' # on Darwin, .bashrc is a symlink to .bash_profile
alias g="google" #alias to the google function in .bash_profile
alias facebook='$OPEN https://www.facebook.com/'
alias youtube='google -youtube'
alias y='youtube'
alias wiki='google wikipedia'
alias kl='kill %1' 
alias p='bpython'
alias ps='ps -a'
alias c='clear'
alias cat='cat -n'
alias code='python /Users/tati/Desktop/GIT/HTTP-Status-Code-CLI/http_status_code_parser.py'
alias todo='/bin/cat ~/.todo'
alias todoa='~/.local/bin/todo.sh APP'
alias todot='~/.local/bin/todo.sh TASK'
alias todof='~/.local/bin/todo.sh FEATURE'
